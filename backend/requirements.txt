# Backend API: python -m uvicorn backend.api_initialization:app --reload --port 8000
# Install: python3 -m pip install -r backend/requirements.txt
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0
numpy
torch
gym>=0.26.0
